import styled from "styled-components";

export const WelcomebackStyle = styled.div`
  font-size: xxx-large;
  font-weight: 700;
`;

export const DescriptionStyle = styled.div`
  padding: 0.3em;
  font-size: larger;
  color: #969696;
`;

export const WalletPwInputStyle = styled.input`
  width: 28em;
  height: 3em;
  margin: 2em;
  padding: 1em;
  border-radius: 10px;
  font-size: larger;
  background: black;
  border-color: white;
`;

export const RecoveryTextStyle = styled.div`
  text-decoration: underline;
`;

export const UnlockButton = styled.div`
  background: #f44336;
  padding: 1em 15em;
  border-radius: 29px;
  font-size: large;
  font-weight: 800;
  margin: 6em;
  cursor: pointer;
`;
